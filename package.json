{
  "name": "placeholderin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && esbuild src/background.ts src/content.ts src/popup/index.ts src/options.ts --bundle --outdir=dist --target=chrome120 --format=iife",
    "dev": "esbuild src/background.ts src/content.ts src/popup/index.ts src/options.ts --bundle --outdir=dist --target=chrome120 --format=iife --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.28.0",
    "typescript": "^5.8.3"
  }
}
